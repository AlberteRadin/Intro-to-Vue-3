const app = Vue.createApp({
    data(){
        return {
            product: "Socks",
            // Solution
            description: "A pair of warm, fuzzy socks"
            // Solution
        }
    }

})
